module.exports = {
    compile: {
        options: {
            baseUrl: 'src/modules',
            dir: "build",
            keepAmdefine: true,
            removeCombined: true,
            useStrict: true
        }
    }
};