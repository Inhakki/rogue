module.exports = {
    compile: {
        options: {
            baseUrl: 'src/modules',
            dir: "build",
            useStrict: true,
            findNestedDependencies: true
        }
    }
};